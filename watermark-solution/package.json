{
  "name": "watermark-solution",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ./build.mjs",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "esbuild": "^0.25.5",
    "rimraf": "^6.0.1"
  }
}
